
services:
  db:
    image: postgres:16
    environment:
      POSTGRES_DB: train
      POSTGRES_USER: app
      POSTGRES_PASSWORD: secret
    ports: ["5432:5432"]
    volumes: [db-data:/var/lib/postgresql/data]

volumes:
  db-data:
